"""Bounded model-improvement experiments (seed 42, ~30 min CPU total).

Nothing here auto-promotes: winners are written as challenger bundles under
models/challengers/ and must pass `python -m ml.benchmark --challenger` before
their params/kind are adopted in ml.config and the pipeline retrained.
"""
